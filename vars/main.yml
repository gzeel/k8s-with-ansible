---
pod_network: "10.244.0.0/16"
timezone: "Europe/Amsterdam"
user: "gerrit"
home: "{{ lookup('env', 'HOME') }}"
k8s_version: "v1.32"
kubectl_bin: "/usr/bin/kubectl"
istioctl_bin: "/usr/bin/istioctl"
istio_version: "1.26.0"  # Update this to the version you want to install
istio_download_location: "/tmp/istio"
istio_install_dir: "/tmp/istio/istio-{{ istio_version }}"
demo_profile_flag: true  # Set to true to install demo profile
